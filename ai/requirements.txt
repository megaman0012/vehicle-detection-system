# AI Service Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Core AI/ML Dependencies
opencv-python==4.8.1.78
numpy>=1.26,<2
psutil==5.9.8

# For communication with backend
requests==2.31.0
aiohttp==3.9.0

# YOLOv8 vehicle detection (requires torch; CPU build installed in the
# Dockerfile from the PyTorch CPU index to avoid pulling CUDA wheels)
ultralytics==8.2.103
torch==2.4.0

# PaddleOCR license plate recognition (PaddleOCR 3.x line, CPU-capable)
paddleocr==3.7.0
paddlepaddle==3.3.1

# For development (optional)
# pytest==7.4.0
# pytest-asyncio==0.23.0
