# AI Service Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Core AI/ML Dependencies (minimal set for testing)
opencv-python==4.8.1.78
numpy>=1.26,<2
psutil==5.9.8

# For communication with backend
requests==2.31.0
aiohttp==3.9.0

# Optional heavy AI stack (uncomment to enable real YOLOv8 + PaddleOCR inference;
# requires a much larger image and, ideally, a GPU at runtime).
# ultralytics==8.2.103
# torch==2.4.0
# paddleocr==2.7.3
# paddlepaddle==2.6.1

# For development (optional)
# pytest==7.4.0
# pytest-asyncio==0.23.0